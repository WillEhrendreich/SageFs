module SageFs.Tests.McpTargetedVerificationToolTests

// Tests for selectTestsForExplicitRun were removed when run_tests was removed.
// MCP is now a read-only observer of live testing; use send_fsharp_code to trigger runs.
