module SageFs.Tests.Tier0CorrectnessTests

// collectResults and RunTestsResult were removed when run_tests was deleted.
// MCP is now a read-only observer of live testing — it cannot trigger test runs.
// These tests tested MCP-side result collection logic that no longer exists.
