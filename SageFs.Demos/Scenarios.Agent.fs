/// Compiling stub for the Agent/MCP scenario (demo-actors-plan.md §1.3/§2.4:
/// `agent-mcp`). `Scenarios.All.fs` already references `scenarios` below —
/// Island F leaves it empty; the Agent island fills it in.
module SageFs.Demos.Scenarios.Agent

let scenarios: SageFs.Demos.Domain.Scenario list = []
