<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>SageFs ? Terminal Noir</title>
<style>
  :root {
    --bg: #0a0a0f;
    --panel: #14141f;
    --raised: #1e1e2e;
    --rule: #2a2a3a;
    --text: #e1e0f8;
    --dim: #6b6b80;
    --dimmer: #4a4a5a;
    --accent: #00d4aa;
    --accent-dim: #00a080;
    --error: #ff6b6b;
    --warn: #ffb86c;
    --blue: #8be9fd;
    --purple: #bd93f9;
    --font: 'JetBrains Mono', 'Cascadia Code', 'Fira Code', ui-monospace, monospace;
  }
  * { box-sizing: border-box; }
  html, body { margin: 0; padding: 0; height: 100%; background: var(--bg); color: var(--text); font-family: var(--font); font-size: 13px; line-height: 1.55; -webkit-font-smoothing: antialiased; }
  body { display: grid; grid-template-rows: 36px 28px 1fr; height: 100vh; overflow: hidden; }

  /* ?? Title bar ??????????????????????????????????????????? */
  .titlebar {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    padding: 0 12px;
    border-bottom: 1px solid var(--rule);
    background: var(--panel);
    gap: 16px;
  }
  .brand { display: flex; align-items: center; gap: 8px; font-weight: 600; letter-spacing: 0.04em; }
  .brand .logo { color: var(--accent); }
  .brand .v { color: var(--dim); font-weight: 400; }
  .tabs { display: flex; gap: 0; overflow-x: auto; }
  .tab {
    display: flex; align-items: center; gap: 8px;
    padding: 0 12px; height: 36px; line-height: 36px;
    border-right: 1px solid var(--rule);
    color: var(--dim); font-size: 12px;
    cursor: pointer; user-select: none;
  }
  .tab.active { background: var(--bg); color: var(--text); }
  .tab .dot { width: 7px; height: 7px; border-radius: 50%; background: var(--accent); box-shadow: 0 0 8px var(--accent); }
  .tab .dot.warm { background: var(--warn); box-shadow: 0 0 8px var(--warn); }
  .tab .dot.dead { background: var(--error); }
  .tab .x { color: var(--dimmer); padding: 0 4px; }
  .tab .x:hover { color: var(--error); }
  .title-actions { display: flex; align-items: center; gap: 8px; font-size: 11px; }
  .pill { border: 1px solid var(--rule); padding: 2px 8px; color: var(--dim); }
  .pill .k { color: var(--accent); }

  /* ?? Health bar ?????????????????????????????????????????? */
  .healthbar {
    display: flex; align-items: center; gap: 14px;
    padding: 0 12px; border-bottom: 1px solid var(--rule);
    background: var(--bg); color: var(--dim); font-size: 11px;
  }
  .healthbar .led { color: var(--accent); }
  .healthbar .sep { color: var(--dimmer); }

  /* ?? Main grid ??????????????????????????????????????????? */
  main {
    display: grid;
    grid-template-columns: 1fr 360px;
    overflow: hidden;
    min-height: 0;
  }
  .col {
    display: flex; flex-direction: column;
    min-width: 0; min-height: 0; overflow: hidden;
  }
  .col-left { border-right: 1px solid var(--rule); }

  /* ?? Output ????????????????????????????????????????????? */
  .pane { display: flex; flex-direction: column; min-height: 0; flex: 1; }
  .pane-h {
    display: flex; align-items: center; justify-content: space-between;
    padding: 8px 12px; border-bottom: 1px solid var(--rule);
    background: var(--panel); color: var(--dim); font-size: 11px;
    text-transform: uppercase; letter-spacing: 0.12em;
  }
  .pane-h .right { display: flex; gap: 6px; }
  .pane-h .right button {
    background: transparent; border: 1px solid var(--rule);
    color: var(--dim); font-family: var(--font); font-size: 10px;
    padding: 2px 6px; cursor: pointer; letter-spacing: 0.1em;
  }
  .pane-h .right button:hover { color: var(--accent); border-color: var(--accent-dim); }
  .output {
    flex: 1; overflow-y: auto; padding: 8px 0;
    font-size: 12.5px; line-height: 1.65;
    scrollbar-width: thin; scrollbar-color: var(--rule) transparent;
  }
  .line { padding: 0 12px; white-space: pre-wrap; word-break: break-all; }
  .line .ts { color: var(--dimmer); margin-right: 8px; }
  .line .tag { color: var(--accent); margin-right: 6px; }
  .line.err .tag { color: var(--error); }
  .line.warn .tag { color: var(--warn); }
  .line.info .tag { color: var(--blue); }
  .line.dim { color: var(--dim); }
  .line.result { color: var(--accent); }
  .line .dim { color: var(--dim); }
  .prompt::before { content: '> '; color: var(--accent); }

  /* ?? Eval area ?????????????????????????????????????????? */
  .eval {
    border-top: 1px solid var(--rule);
    background: var(--panel);
    display: grid; grid-template-rows: auto 1fr auto;
  }
  .eval-h {
    display: flex; align-items: center; justify-content: space-between;
    padding: 6px 12px; color: var(--dim); font-size: 11px;
    letter-spacing: 0.12em; text-transform: uppercase;
  }
  .eval-h .meta { color: var(--dimmer); text-transform: none; letter-spacing: 0; font-size: 10.5px; }
  .eval-body {
    display: flex; align-items: flex-start; gap: 8px;
    padding: 8px 12px; min-height: 64px;
  }
  .eval-body .gt { color: var(--accent); font-size: 14px; padding-top: 2px; user-select: none; }
  .eval-body textarea {
    flex: 1; background: transparent; color: var(--text);
    border: 0; outline: 0; resize: none; font-family: var(--font);
    font-size: 13px; line-height: 1.55; min-height: 48px;
  }
  .eval-body textarea::placeholder { color: var(--dimmer); }
  .eval-actions {
    display: flex; align-items: center; gap: 8px;
    padding: 6px 12px; border-top: 1px solid var(--rule);
  }
  .btn {
    background: transparent; color: var(--text);
    border: 1px solid var(--rule); padding: 4px 10px;
    font-family: var(--font); font-size: 11px; letter-spacing: 0.1em;
    text-transform: uppercase; cursor: pointer;
  }
  .btn:hover { border-color: var(--accent-dim); color: var(--accent); }
  .btn.primary { background: var(--accent); color: var(--bg); border-color: var(--accent); }
  .btn.primary:hover { background: var(--accent-dim); border-color: var(--accent-dim); }
  .btn.danger { color: var(--error); border-color: #5a2a30; }
  .btn.danger:hover { background: #2a1418; }
  .eval-meta { margin-left: auto; color: var(--dim); font-size: 10.5px; }
  .eval-meta kbd {
    background: var(--raised); color: var(--dim);
    padding: 1px 4px; border: 1px solid var(--rule); font-family: var(--font);
    font-size: 10px; border-radius: 2px;
  }

  /* ?? Sidebar ???????????????????????????????????????????? */
  .sb-h {
    padding: 10px 12px; border-bottom: 1px solid var(--rule);
    background: var(--panel);
    display: flex; align-items: baseline; justify-content: space-between;
  }
  .sb-h .label { font-size: 10.5px; color: var(--dim); text-transform: uppercase; letter-spacing: 0.18em; }
  .sb-h .count { color: var(--accent); font-size: 11px; }
  .sb-section { border-bottom: 1px solid var(--rule); }
  .sb-section-h {
    display: flex; align-items: center; justify-content: space-between;
    padding: 8px 12px; background: var(--bg);
    color: var(--dim); font-size: 10.5px;
    text-transform: uppercase; letter-spacing: 0.16em; cursor: pointer;
    user-select: none;
  }
  .sb-section-h:hover { color: var(--text); }
  .sb-section-h .right { color: var(--dimmer); font-size: 10.5px; letter-spacing: 0; text-transform: none; }
  .sb-section-h .toggle { color: var(--dimmer); }
  .sb-body { padding: 6px 8px; display: flex; flex-direction: column; gap: 6px; }

  /* ?? Session card ?????????????????????????????????????? */
  .sess {
    border: 1px solid var(--rule);
    background: var(--bg);
    display: grid; grid-template-columns: 1fr auto; gap: 4px;
    padding: 8px 10px;
  }
  .sess.active { border-color: var(--accent-dim); box-shadow: inset 2px 0 0 var(--accent); }
  .sess .row1 { display: flex; align-items: center; gap: 8px; font-size: 12px; }
  .sess .id { color: var(--text); font-weight: 600; letter-spacing: 0.02em; }
  .sess .badge {
    font-size: 9.5px; padding: 1px 5px; text-transform: uppercase;
    letter-spacing: 0.1em; border: 1px solid var(--rule);
  }
  .sess .badge.run { color: var(--accent); border-color: var(--accent-dim); }
  .sess .badge.warm { color: var(--warn); border-color: #6a4d20; }
  .sess .badge.dead { color: var(--error); border-color: #6a2a30; }
  .sess .row2 {
    display: flex; align-items: center; gap: 6px;
    color: var(--dim); font-size: 10.5px;
  }
  .sess .cwd {
    color: var(--dim); font-size: 10.5px; padding-top: 2px;
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
    max-width: 240px;
  }
  .sess .proj {
    display: inline-block; padding: 1px 6px; margin-top: 4px;
    background: var(--raised); color: var(--dim);
    font-size: 10px; border: 1px solid var(--rule);
  }
  .sess .row3 { color: var(--dimmer); font-size: 10px; padding-top: 2px; }
  .sess .acts { display: flex; gap: 2px; }
  .sess .acts button {
    background: transparent; border: 1px solid transparent;
    color: var(--dim); font-family: var(--font); font-size: 12px;
    width: 22px; height: 22px; cursor: pointer; padding: 0;
  }
  .sess .acts button:hover { color: var(--accent); border-color: var(--rule); }
  .sess .acts button.kill:hover { color: var(--error); }
  .sess-meta {
    display: flex; gap: 10px; padding: 4px 12px;
    color: var(--dimmer); font-size: 10px; border-top: 1px solid var(--rule);
    background: var(--bg);
  }
  .sess-meta .k { color: var(--dim); }

  /* ?? Bottom status strip ??????????????????????????????? */
  .statusbar {
    display: flex; align-items: center; gap: 16px;
    padding: 4px 12px; border-top: 1px solid var(--rule);
    background: var(--panel); color: var(--dim); font-size: 10.5px;
  }
  .statusbar .led { color: var(--accent); }
  .statusbar .right { margin-left: auto; }

  /* ?? Signature: blinking block cursor ??????????????????? */
  @keyframes blink { 0%,49% { opacity: 1; } 50%,100% { opacity: 0; } }
  .cursor {
    display: inline-block; width: 7px; height: 14px;
    background: var(--accent); vertical-align: -2px;
    margin-left: 1px; animation: blink 1s steps(1) infinite;
  }
  .cursor.soft { animation-duration: 1.4s; }

  /* responsive */
  @media (max-width: 900px) {
    main { grid-template-columns: 1fr; }
    .col-right { display: none; }
  }
</style>
</head>
<body>
  <div class="titlebar">
    <div class="brand">
      <span class="logo">?</span><span>SageFs</span>
      <span class="v">v0.6.303.0</span>
    </div>
    <div class="tabs">
      <div class="tab"><span class="dot"></span><span>87ec774d</span><span class="dim" style="color:var(--dim)">REPL</span><span class="x">?</span></div>
      <div class="tab active"><span class="dot"></span><span>e0fcb2a4</span><span class="dim" style="color:var(--dim)">REPL</span><span class="x">?</span></div>
      <div class="tab"><span class="dot warm"></span><span>99f843b9</span><span class="dim" style="color:var(--dim)">Live</span><span class="x">?</span></div>
      <div class="tab" style="color:var(--dimmer)">+ New</div>
    </div>
    <div class="title-actions">
      <span class="pill">session <span class="k">e0fcb2a4</span></span>
      <span class="pill">cwd <span class="k">~/samples/WebappDatastar</span></span>
      <span class="pill">? K</span>
    </div>
  </div>

  <div class="healthbar">
    <span><span class="led">?</span> healthy</span>
    <span class="sep">?</span>
    <span>up 9m</span>
    <span class="sep">?</span>
    <span>mem 7899MB</span>
    <span class="sep">?</span>
    <span><span class="led">?</span> SageFs.Samples.WebappDatastar.fsproj</span>
    <span class="sep">?</span>
    <span><span style="color:var(--dim)">?</span> SageTech.fsproj</span>
    <span style="margin-left:auto; color:var(--dim)">120 evals ? avg 14ms ? max 2.3s</span>
  </div>

  <main>
    <div class="col col-left">
      <div class="pane">
        <div class="pane-h">
          <span>? output</span>
          <div class="right">
            <button>follow</button>
            <button>copy</button>
            <button>clear</button>
          </div>
        </div>
        <div class="output">
          <div class="line dim"><span class="ts">16:21:00</span><span class="tag">?</span>[1/4] FSI session created</div>
          <div class="line dim"><span class="ts">16:21:00</span><span class="tag">?</span>[2/4] Scanned 3 source files</div>
          <div class="line dim"><span class="ts">16:21:00</span><span class="tag">?</span>[3/4] Scanned assemblies, opening 8 namespaces</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[1/8] ? open System (116ms)</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[2/8] ? open System.Reflection (116ms)</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[3/8] ? open Falco (116ms)</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[4/8] ? open Falco.Routing (116ms)</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[5/8] ? open Falco.Markup (116ms)</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[6/8] ? open Falco.Datastar (116ms)</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[7/8] ? open Microsoft.AspNetCore.Builder (116ms)</div>
          <div class="line dim"><span class="ts">16:21:01</span><span class="tag">?</span>[8/8] ? open SageFs (116ms)</div>
          <div class="line info"><span class="ts">16:21:01</span><span class="tag">?</span>warm-up complete ? 3 838ms ? 8 namespaces</div>
          <div class="line"><span class="ts">16:21:15</span><span class="tag">?</span>let greet name = sprintf "hello, %s" name</div>
          <div class="line result"><span class="ts">16:21:15</span><span class="tag">val</span>greet : name:string -> string</div>
          <div class="line"><span class="ts">16:21:18</span><span class="tag">?</span>greet "ada"</div>
          <div class="line result"><span class="ts">16:21:18</span><span class="tag">?</span>"hello, ada"</div>
          <div class="line"><span class="ts">16:21:24</span><span class="tag">?</span>type Cart = { items: string list; total: decimal }</div>
          <div class="line result"><span class="ts">16:21:24</span><span class="tag">type</span>Cart = { items: string list; total: decimal }</div>
          <div class="line"><span class="ts">16:21:31</span><span class="tag">?</span>let add (c: Cart) item = { c with items = item :: c.items; total = c.total + 9.99m }</div>
          <div class="line result"><span class="ts">16:21:31</span><span class="tag">val</span>add : c:Cart -> item:string -> Cart</div>
          <div class="line"><span class="ts">16:21:33</span><span class="tag">?</span>{ items = []; total = 0m } |> add "book" |> add "pen"</div>
          <div class="line result"><span class="ts">16:21:33</span><span class="tag">?</span>{ items = ["pen"; "book"]; total = 19.98M }</div>
          <div class="line warn"><span class="ts">16:21:40</span><span class="tag">?</span>0 tests discovered ? enable Live Testing to scan</div>
          <div class="line"><span class="ts">16:21:48</span><span class="tag">?</span>[<EntryPoint>]</div>
          <div class="line err"><span class="ts">16:21:48</span><span class="tag">?</span>error FS0039: The type 'EntryPoint' is not defined</div>
        </div>
      </div>
      <div class="eval">
        <div class="eval-h">
          <span>? evaluate <span class="meta">? fsi ? e0fcb2a4</span></span>
          <span class="meta">2L ? 47c</span>
        </div>
        <div class="eval-body">
          <span class="gt">?</span>
          <textarea autofocus spellcheck="false" placeholder="Enter F# code? ;; auto-appended. Alt+Enter to eval, Tab to indent.">let total (c: Cart) = c.items |> List.length, c.total</textarea>
        </div>
        <div class="eval-actions">
          <button class="btn primary">Run ? ??</button>
          <button class="btn">Save to file</button>
          <button class="btn">History</button>
          <span class="eval-meta">tip: <kbd>Tab</kbd> indent ? <kbd>?</kbd> history ? <kbd>Ctrl+L</kbd> clear</span>
        </div>
      </div>
    </div>

    <div class="col col-right">
      <div class="sb-h">
        <span class="label">Sessions</span>
        <span class="count">2 of 3</span>
      </div>
      <div class="sb-section">
        <div class="sb-section-h"><span>? active</span><span class="right">?1</span></div>
        <div class="sb-body">
          <div class="sess active">
            <div>
              <div class="row1"><span class="id">e0fcb2a4</span><span class="badge run">run</span></div>
              <div class="row2"><span style="color:var(--accent)">?</span> active ? <span style="color:var(--dim)">? 6m</span></div>
              <div class="cwd" title="C:\Code\Repos\SageFs\samples\demos\SageFs.Samples.WebappDatastar">~/samples/demos/SageFs.Samples.WebappDatastar</div>
              <div><span class="proj">SageFs.Samples.WebappDatastar</span></div>
              <div class="row3">last eval: 16:21:48 ? 14ms</div>
            </div>
            <div class="acts">
              <button title="focus">?</button>
              <button title="restart">?</button>
              <button title="stop" class="kill">?</button>
              <button title="purge" class="kill">?</button>
            </div>
          </div>
        </div>
      </div>
      <div class="sb-section">
        <div class="sb-section-h"><span>? standby</span><span class="right">?2</span></div>
        <div class="sb-body">
          <div class="sess">
            <div>
              <div class="row1"><span class="id">87ec774d</span><span class="badge">idle</span></div>
              <div class="row2"><span style="color:var(--dim)">?</span> standby ? <span style="color:var(--dim)">? 9m</span></div>
              <div class="cwd">~/samples/demos/SageFs.Samples.WebappDatastar</div>
              <div><span class="proj">SageFs.Samples.WebappDatastar</span></div>
              <div class="row3">last eval: 6m ago</div>
            </div>
            <div class="acts">
              <button>?</button>
              <button>?</button>
              <button>?</button>
              <button class="kill">?</button>
            </div>
          </div>
          <div class="sess">
            <div>
              <div class="row1"><span class="id">99f843b9</span><span class="badge warm">warm</span></div>
              <div class="row2"><span style="color:var(--warn)">?</span> warming ? <span style="color:var(--dim)">? 0m</span></div>
              <div class="cwd">~/repos/SageTech</div>
              <div><span class="proj">SageTech</span></div>
              <div class="row3">last eval: 0m ago</div>
            </div>
            <div class="acts">
              <button>?</button>
              <button>?</button>
              <button class="kill">?</button>
              <button class="kill">?</button>
            </div>
          </div>
        </div>
      </div>
      <div class="sess-meta">
        <span><span class="k">switch</span> ?</span>
        <span><span class="k">stop</span> ?</span>
        <span><span class="k">dispose</span> ?</span>
        <span><span class="k">purge</span> ?</span>
        <span style="margin-left:auto"><button class="btn danger" style="padding:2px 8px; font-size:10px">stop others</button></span>
      </div>
      <div class="sb-section">
        <div class="sb-section-h"><span>? hot reload</span><span class="right"><span class="dot" style="color:var(--dim)">?</span> off ? 0/2</span></div>
        <div class="sb-body">
          <div style="display:flex; gap:6px">
            <button class="btn">Watch all</button>
            <button class="btn">Unwatch all</button>
          </div>
          <div style="font-size:11px; color:var(--dim); padding: 2px 4px;">? 2 files ? .fs</div>
        </div>
      </div>
      <div class="sb-section">
        <div class="sb-section-h"><span>? live testing</span><span class="right">off</span></div>
        <div class="sb-body">
          <div style="font-size:11px; color:var(--dim); padding: 2px 4px;">Enable to discover and run tests on save.</div>
          <button class="btn">Enable</button>
        </div>
      </div>
      <div class="sb-section">
        <div class="sb-section-h"><span>? bindings</span><span class="right">0</span></div>
      </div>
      <div class="sb-section">
        <div class="sb-section-h"><span>? session context</span><span class="right">8 ns ? 0 fail</span></div>
      </div>
      <div class="sb-section">
        <div class="sb-section-h"><span>+ new session</span></div>
      </div>
    </div>
  </main>

  <div class="statusbar">
    <span><span class="led">?</span> fsi ready</span>
    <span class="sep">?</span>
    <span>10240MB resident</span>
    <span class="sep">?</span>
    <span>hot reload armed</span>
    <span class="right">awaiting input<span class="cursor soft"></span></span>
  </div>
</body>
</html>

